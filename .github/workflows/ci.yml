name: CI (Python)
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Import smoke (src)
        env:
          PYTHONPATH: src
        run: python -c "import quip_api_es, fastapi; print('import ok')"
